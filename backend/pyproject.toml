[project]
name = "health-recommender-backend"
version = "0.1.0"
description = "FastAPI backend for Health Recommender"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "pydantic",
]
